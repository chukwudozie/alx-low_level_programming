#include <stdio.h>

/**
 * main - Entry point
 * Description: print in reverse
 * Return: Always 0 (success)
 */

int main(void)
{
	int num = 'z';

	while (num > 'a')
	{
		putchar(num);
		c--;
	}

	putchar('\n');

	return (0);
}
