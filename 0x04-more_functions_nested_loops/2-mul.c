#include "main.h"
#include <stdio.h>

/**
 * mul - function to multiply two numbers
 * @a: input number
 * @b: input number
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
